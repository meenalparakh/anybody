EVAL: true
EVAL_ON_TEST: false
IS_FINETUNING: true

# must be provided through command line argument
# EVAL_CHECKPOINT: LOGS_PATH/path/to/checkpoints/agent.pt

FORCE_PROBLEM_SPEC_GEN: false
SEARCH_CHECKPOINT: false

TRAINER:
  TIMESTEPS: 50000
  EVAL_TIMESTEPS_INTERVAL: 14000
  EVAL_TIMESTEPS: 500
  VIDEO_RENDER: false

AGENT:
  EXPERIMENT:
    CHECKPOINT_INTERVAL: 5000
    WRITE_INTERVAL: 1000

CURRICULUM:
  ACTIVE: false
